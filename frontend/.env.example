# API proxy target for the Vite dev server.
# Requests to /api/* are forwarded to this URL.
# Override in .env.local to point at a different backend (e.g. a Docker Compose service).
VITE_API_PROXY_TARGET=http://localhost:3000
